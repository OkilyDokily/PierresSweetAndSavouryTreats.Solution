@using PierresSweetAndSavouryTreats.Models

@{
  Layout = "_Layout";
}

<p>@Html.ActionLink("Create","Create")</p>
@foreach(Treat treat in @Model)
{
  <p>@Html.ActionLink(@treat.Name,"Details", new {id = @treat.Id})</p>
}
