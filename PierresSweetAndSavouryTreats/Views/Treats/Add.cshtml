@{
  Layout = "_Layout";
}

@using (Html.BeginForm())
{
  @Html.DropDownList("FlavorId",null,new {@class="btn btn-default dropdown-toggle"})
  <input type="submit" value="Add flavor to treat" class="btn">
}